<!-- show errors for any user objects -->
<% if @user && @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@user.errors.count, "error") %>.
    </div>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li>* <%= msg %></li>
      <% end %>
    </ul>
  </div>
<!-- show errors for any family objects -->
<% elsif @family && @family.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@family.errors.count, "error") %>.
    </div>
    <ul>
      <% @family.errors.full_messages.each do |msg| %>
        <li>* <%= msg %></li>
      <% end %>
    </ul>
  </div>
<!-- show errors for any activity objects -->
<% elsif @activity && @activity.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@activity.errors.count, "error") %>.
    </div>
    <ul>
      <% @activity.errors.full_messages.each do |msg| %>
        <li>* <%= msg %></li>
      <% end %>
    </ul>
  </div>
<!-- show errors for any ward objects -->
<% elsif @ward && @ward.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@ward.errors.count, "error") %>.
    </div>
    <ul>
      <% @ward.errors.full_messages.each do |msg| %>
        <li>* <%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>